# 数据挖掘大作业所需依赖
# 安装：uv pip install -r requirements.txt  或  pip install -r requirements.txt

numpy
pandas
matplotlib
scikit-learn
torch
Pillow
scikit-image

# OpenCV 仅用于图像读取与绘制（cv2.HOGDescriptor 已在 opencv-python 5.x 移除，
# 飞机检测改用 skimage.feature.hog 实现，因此无需 contrib 变体）。
opencv-python-headless
