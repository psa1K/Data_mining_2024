# 数据挖掘大作业所需依赖（已锁定版本，保证可复现）
# 安装：uv pip install -r requirements.txt  或  pip install -r requirements.txt

numpy==2.5.2
pandas==3.0.5
matplotlib==3.11.1
scikit-learn==1.9.0
torch==2.13.0
Pillow==12.3.0
scikit-image==0.26.0

# OpenCV 仅用于图像读取与绘制；HOG 特征由 scikit-image 实现。
opencv-python-headless==5.0.0
