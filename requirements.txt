# 数据挖掘大作业所需依赖
# 安装：uv pip install -r requirements.txt  或  pip install -r requirements.txt

numpy
pandas
matplotlib
scikit-learn
torch
Pillow

# 飞机检测使用 cv2.HOGDescriptor，该接口在 opencv-python（主包）5.x 中已被移除，
# 仅存在于 opencv-contrib-python(-headless) 中，因此必须安装 contrib 变体。
opencv-contrib-python-headless>=5.0
